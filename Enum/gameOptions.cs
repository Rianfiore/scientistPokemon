namespace ScientistPokemon.Enum
{
    public enum eInitGameOptions
    {
        newGame = 1,

        loadGame = 2,

        exitGame = 3,
    }

    public enum eGameOptions
    {
        teste = 1,
        tste2 = 2,
    }
}